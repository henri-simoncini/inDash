import { RecoverForm } from "@/components/auth/recover-form";

export const metadata = { title: "Recuperar senha — inDash" };

export default function RecuperarSenhaPage() {
  return <RecoverForm />;
}
